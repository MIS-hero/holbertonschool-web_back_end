#!/usr/bin/env python3
"""A module provides a function that concatenates two strings."""


def concat(str1: str, str2: str) -> str:
    """Concatenate two strings.

    Args:
        str1(str): first string
        str2(str): second string

    Returns:
        str: concatenation of the two strings
    """
    return str1 + str2
